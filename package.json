{
  "name": "redux-gateway",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha",
    "docs": "typedoc --out ./docs .",
    "build": "npm install && typings install && tsc && cd demo && webpack",
    "all": "npm run build && npm run test && npm run docs && touch ./docs/.nojekyll"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "immutable": "^3.8.1",
    "redux": "^3.6.0",
    "socket.io": "^1.7.1",
    "socket.io-client": "^1.7.1"
  },
  "typings": "build/index",
  "main": "build/index.js",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "ts-loader": "^1.3.0",
    "typedoc-default-themes": "0.4.0",
    "typescript": "^2.1.4"
  }
}
